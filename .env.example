# Development: Vite proxies /api and /static to the backend.
API_PROXY_TARGET=http://127.0.0.1:8000
# Production: empty uses same-origin reverse proxy; otherwise set the API origin.
VITE_API_BASE_URL=
